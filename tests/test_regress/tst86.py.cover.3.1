+ def boost_by(extra):
+     def decorator(func):
+         def wrapper(arg):
+             return extra*func(arg)
+         return wrapper
+     return decorator
0 
+ @boost_by(10)
0 def boosted(arg):
+     return arg*2
0 
+ assert boosted(10) == 200
